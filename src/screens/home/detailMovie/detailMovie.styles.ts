import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  header: {
    position: 'absolute',
    top: 0,
    zIndex: 1,
    height: 50,
    width: '100%',
    padding: 10,
    flexDirection: 'row',
    alignItems: 'center'
  }
});
